const {spawnSync}=require('child_process');function check(cmd,args=['--version']){const r=spawnSync(cmd,args,{shell:process.platform==='win32',encoding:'utf8'});return r.status===0?`OK ${cmd}: ${(r.stdout||r.stderr).trim().split('\n')[0]}`:`FAIL ${cmd}`}console.log(check('node'));console.log(check('npm'));console.log(check('ffmpeg'));console.log(check('npx',['hyperframes@latest','--version']));
